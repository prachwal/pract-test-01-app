{
  "name": "pract-test-01-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "build:styles": "gulp",
    "styles": "gulp styles",
    "clean:dist": "gulp cleanDist",
    "watch:styles": "gulp watch",
    "dev:styles": "gulp watch &",
    "postcss:info": "npx autoprefixer --info",
    "postcss:test": "node -e \"console.log('PostCSS config loaded successfully')\"",
    "postcss:browsers": "echo 'Checking browser support...' && npx autoprefixer --info | head -5"
  },
  "dependencies": {
    "@preact/signals": "^2.5.0",
    "preact": "^10.27.2"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.2",
    "@types/node": "^24.10.1",
    "autoprefixer": "^10.4.20",
    "gulp": "^5.0.1",
    "gulp-clean": "^0.4.0",
    "gulp-postcss": "^10.0.0",
    "gulp-sass": "^6.0.1",
    "gulp-sourcemaps": "^3.0.0",
    "postcss": "^8.5.1",
    "sass": "^1.94.0",
    "typescript": "~5.9.3",
    "vite": "^7.2.2"
  }
}
